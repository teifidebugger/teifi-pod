export {
    mapStateToUATColumn,
    findStateForUATColumn,
    buildOpenTransitions,
} from '@/lib/uat-mapping'
export type {
    UATColumn,
    UATColumnConfig,
    AllowedTransitions,
} from '@/lib/uat-mapping'
